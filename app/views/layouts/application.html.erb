<!DOCTYPE html>
<html lang="ja">
  <head>
        <title>みんなの子供おやつレシピ</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <!-- ヘッダー -->
    <header class="header">
      <div class="container">
        <div class="logo">
              <%= link_to root_path do %>
                <h1>🍪 みんなの子供おやつレシピ</h1>
              <% end %>
        </div>
        <nav class="nav">
          <% if user_signed_in? %>
            <div class="user-info">
              <%= link_to "#{current_user.nickname}さん", mypage_path, class: "user-name-link" %>
              <%= button_to "ログアウト", destroy_user_session_path, method: :delete, class: "btn btn-outline" %>
            </div>
          <% else %>
            <div class="nav-buttons">
              <%= link_to "ログイン", new_user_session_path, class: "btn btn-outline" %>
              <%= link_to "新規登録", new_user_registration_path, class: "btn btn-primary" %>
            </div>
          <% end %>
        </nav>
      </div>
    </header>

    <!-- メインコンテンツ -->
    <main class="main">
      <%= yield %>
    </main>

    <!-- フッター -->
    <footer class="footer">
      <div class="container">
            <p>&copy; 2025 みんなの子供おやつレシピ. 子供が喜ぶおやつレシピ共有アプリ</p>
      </div>
    </footer>
  </body>
</html>
