<div class="container">
  <div class="recipe-form-container">
    <h2>おやつレシピを編集</h2>

    <%= form_with model: @recipe_form, url: recipe_path(@recipe), method: :patch, local: true, multipart: true, class: "recipe-form" do |form| %>
      <% if @recipe_form.errors.any? %>
        <div class="error-messages">
          <h3><%= pluralize(@recipe_form.errors.count, "error") %> prohibited this recipe from being saved:</h3>
          <ul>
            <% @recipe_form.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="form-group">
        <%= form.label :title, "レシピタイトル" %>
        <%= form.text_field :title, required: true %>
      </div>

      <div class="form-group">
        <%= form.label :description, "レシピ詳細" %>
        <%= form.text_area :description, rows: 15, required: true %>
      </div>

      <div class="form-group">
        <%= form.label :image, "レシピ画像（新しい画像を選択する場合のみ）" %>
        <% if @recipe.image.attached? %>
          <div class="current-image">
            <p>現在の画像:</p>
            <%= image_tag @recipe.image, style: "max-width: 200px; height: auto; margin-bottom: 10px;" %>
          </div>
        <% end %>
        <%= form.file_field :image, accept: "image/*", class: "file-input" %>
        <small class="form-help">新しい画像を選択すると、現在の画像が置き換わります。JPG、PNG、GIF形式の画像ファイルを選択してください。最大5MBまでアップロード可能です。</small>
      </div>
      
      <div class="form-group">
        <%= form.label :tag_ids, "タグ（任意）" %>
        
        <!-- 既存タグの選択 -->
        <div class="existing-tags">
          <h4>既存のタグから選択</h4>
          <div class="tag-selection">
            <% Tag.all.each do |tag| %>
              <label class="tag-checkbox">
                <%= form.check_box :tag_ids, { multiple: true, checked: @recipe.tags.include?(tag) }, tag.id, nil %>
                <span class="tag-label"><%= tag.name %></span>
              </label>
            <% end %>
          </div>
        </div>
        
        <!-- 新しいタグの入力 -->
        <div class="new-tag-input">
          <h4>新しいタグを追加</h4>
          <div class="tag-input-group">
            <%= form.text_field :new_tag_names, 
                placeholder: "例: 野菜, 簡単, 子供が好き（カンマ区切りで複数入力可能）", 
                class: "tag-input" %>
            <small class="form-help">新しいタグをカンマ区切りで入力してください。例: 野菜, 簡単, 子供が好き</small>
          </div>
        </div>
        
        <small class="form-help">既存のタグから選択するか、新しいタグを入力してください。</small>
      </div>

      <div class="form-actions">
        <%= form.submit "おやつレシピを更新", class: "btn btn-primary" %>
        <%= link_to "キャンセル", recipes_path, class: "btn btn-secondary" %>
      </div>
    <% end %>
  </div>
</div>
