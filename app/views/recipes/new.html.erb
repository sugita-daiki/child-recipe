<div class="container">
  <div class="recipe-form-container">
        <h2>新しいおやつレシピを投稿</h2>
    
    <%= form_with model: @recipe_form, url: recipes_path, method: :post, local: true, multipart: true, class: "recipe-form" do |form| %>
      <% if @recipe_form.errors.any? %>
        <div class="error-messages">
          <h3><%= pluralize(@recipe_form.errors.count, "error") %> prohibited this recipe from being saved:</h3>
          <ul>
            <% @recipe_form.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>
      
      <div class="form-group">
        <%= form.label :title, "レシピタイトル" %>
        <%= form.text_field :title, placeholder: "例: 子供が喜ぶ！ふわふわクッキー", required: true %>
      </div>
      
      <div class="form-group">
        <%= form.label :description, "レシピ詳細" %>
        <%= form.text_area :description, 
            placeholder: "【材料】\n・薄力粉 100g\n・バター 50g\n・砂糖 30g\n・卵 1個\n\n【作り方】\n1. バターを室温で柔らかくする\n2. 砂糖を加えて混ぜる\n3. 卵を加えて混ぜる\n4. 薄力粉を加えて混ぜる\n5. オーブンで15分焼く\n\n子供が喜ぶ甘いクッキーです。", 
            rows: 15, required: true %>
      </div>
      
      <div class="form-group">
        <%= form.label :image, "レシピ画像（必須）" %>
        <%= form.file_field :image, accept: "image/*", class: "file-input", required: true %>
        <small class="form-help">JPG、PNG、GIF形式の画像ファイルを選択してください。最大5MBまでアップロード可能です。</small>
      </div>
      
      <div class="form-group">
        <%= form.label :tag_ids, "タグ（任意）" %>
        
        <!-- 既存タグの選択 -->
        <div class="existing-tags">
          <h4>既存のタグから選択</h4>
          <div class="tag-selection">
            <% Tag.all.each do |tag| %>
              <label class="tag-checkbox">
                <%= form.check_box :tag_ids, { multiple: true }, tag.id, nil %>
                <span class="tag-label"><%= tag.name %></span>
              </label>
            <% end %>
          </div>
        </div>
        
        <!-- 新しいタグの入力 -->
        <div class="new-tag-input">
          <h4>新しいタグを追加</h4>
          <div class="tag-input-group">
            <%= form.text_field :new_tag_names, 
                placeholder: "例: 野菜, 簡単, 子供が好き（カンマ区切りで複数入力可能）", 
                class: "tag-input" %>
            <small class="form-help">新しいタグをカンマ区切りで入力してください。例: 野菜, 簡単, 子供が好き</small>
          </div>
        </div>
        
        <small class="form-help">既存のタグから選択するか、新しいタグを入力してください。</small>
      </div>
      
      <div class="form-actions">
        <%= form.submit "おやつレシピを投稿", class: "btn btn-primary" %>
        <%= link_to "キャンセル", recipes_path, class: "btn btn-outline" %>
      </div>
    <% end %>
  </div>
</div>
